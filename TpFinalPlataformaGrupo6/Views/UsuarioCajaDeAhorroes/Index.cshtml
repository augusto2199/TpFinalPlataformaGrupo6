@model IEnumerable<TpFinalPlataformaGrupo6.Models.UsuarioCajaDeAhorro>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cajaAhorro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.usuario.apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cajaAhorro.id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_UsuarioCaja">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_UsuarioCaja">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_UsuarioCaja">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
